
### Tapir's markdown (TMD) format

Run `zig build` to build the `tmd` command.
Now it only supports the folloiwng commands:
* `tmd render foo.tmd bar.tmd`
* `tmd render --full-html foo.tmd bar.tmd`

Please visit __https://tmd.tapirgames.com__ for more information.
